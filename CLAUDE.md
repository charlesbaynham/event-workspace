# Claude Code adapter

The contract is imported from `AGENTS.md` (in an event workspace, the shipped
contract; in the upstream template, the maintainer guide); the event's own
rules and the memory digest are imported separately so they are present from
the first turn.

@AGENTS.md
@EVENT.md
@memory/digest.md
